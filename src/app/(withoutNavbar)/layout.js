const Layout = ({ children }) => {
  return (
    <div>
      {children}
      <h1 className="text-4xl font-bold text-center mt-auto min-h-screen">
        Footer
      </h1>
    </div>
  );
};

export default Layout;
